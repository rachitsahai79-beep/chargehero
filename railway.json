{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "context": "chargehero-backend"
  },
  "deploy": {
    "startCommand": "python -m uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "always",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 10
  }
}
